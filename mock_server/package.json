{
  "name": "vehicle-json-server",
  "version": "1.0.0",
  "description": "JSON Server for Vehicle Management",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "json-server": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
